#!/bin/bash

cp gitignore ~/.gitignore
git config --global core.excludesfile ~/.gitignore

git config --global user.name "Jeremy Kun"
git config --global user.email kun.jeremy@gmail.com
git config --global core.editor vim
git config --global github.user j2kun
